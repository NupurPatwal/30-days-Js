// Write a program that uses the ternary operator to check if a number is positive or negative and log the result to the console.

let num1 = -13;

let print = num1 > 0 ? "positive" : "negative";

console.log(print);
