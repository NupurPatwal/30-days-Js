/*
<ul> 
    Task 11:</strong> Write a program that uses the <code>&amp;&amp;</code> operator to combine two conditions and log the result to the console.</li> 
    
    Task 12:</strong> Write a program that uses the <code>||</code> operator to combine two conditions and log the result to the console.</li> 
    
    Task 13:</strong> Write a program that uses the <code>!</code> operator to negate a condition and log the result to the console.</li> 
   </ul>
*/

let condition1 = 23;
let condition2 = 32;

console.log(condition1 > 0 && condition2 > 0);
console.log(condition1 > 0 || condition2 > 0);
console.log(!(condition1 > 0 && condition2 > 0));
console.log(!(condition1 > 0 || condition2 > 0));
